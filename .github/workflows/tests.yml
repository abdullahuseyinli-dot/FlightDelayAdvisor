name: quality-gates

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          lfs: false
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
      - name: Install package and test dependencies
        run: python -m pip install -e ".[dev]"
      - name: Compile Python sources
        run: python -m compileall -q app.py src tools tests
      - name: Lint research package and tests
        run: python -m ruff check src/flightdelaybench tests/test_research_contracts.py
      - name: Test source-only repository
        run: python -m pytest -q -m "not integration and not confirmation"
      - name: Validate repository evidence
        run: python tools/validate_repository.py
