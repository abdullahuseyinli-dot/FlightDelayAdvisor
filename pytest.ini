[pytest]
addopts = -ra -m "not integration"
testpaths = tests
markers =
    integration: requires materialized Git LFS data and model artifacts
    slow: runs full evaluation or plot-generation scripts
    confirmation: may access locked confirmation outcomes
